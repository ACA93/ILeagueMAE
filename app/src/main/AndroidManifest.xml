<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.johnny.ileague">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".ChooseSport" />
        <activity android:name=".ChooseMode" />
        <activity
            android:name=".EnterNames"
            android:label="@string/title_activity_enter_names"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ModeLeague" />
        <activity android:name=".EnterResults"></activity>
    </application>

</manifest>